import React, { Component } from 'react';
import Router from './Router';

class Main extends Component<{}>
{
    render()
    {
        return (
            <Router/>
        );
    }
}

export default Main;
